services:
  deploy-gui:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - ENCRYPT_KEY=${ENCRYPT_KEY}
      - NODE_ENV=production
    restart: unless-stopped
